---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - frontend/index.html
  - frontend/src/App.tsx
  - frontend/src/lib/auth.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/api.test.ts
  - frontend/src/components/landing/LandingPage.tsx
  - frontend/src/components/app-shell/AppShell.tsx
  - frontend/src/components/app-shell/ReAuthModal.tsx
  - frontend/src/types/google.d.ts
  - frontend/.env.example
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, UI-01]

must_haves:
  truths:
    - "User can click Sign in with Google and receive an access token with drive.readonly scope"
    - "Access token is stored in sessionStorage and attached as Authorization header to API calls"
    - "User sees re-auth modal when token expires (403 from API), sessionStorage cleared"
    - "Landing page shows sign-in button with privacy messaging"
    - "Post-auth shows app shell with skeleton shimmer then content"
  artifacts:
    - path: "frontend/src/lib/auth.ts"
      provides: "Token Client initialization and requestToken"
      exports: ["initAuth", "requestToken"]
    - path: "frontend/src/lib/api.ts"
      provides: "Auth-aware fetch wrapper detecting 403"
      exports: ["apiFetch"]
    - path: "frontend/src/components/landing/LandingPage.tsx"
      provides: "Landing page with sign-in button and error handling"
    - path: "frontend/src/components/app-shell/AppShell.tsx"
      provides: "Post-auth app shell with skeleton loading"
    - path: "frontend/src/components/app-shell/ReAuthModal.tsx"
      provides: "Modal dialog for expired token re-auth"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "sessionStorage"
      via: "getItem/removeItem google_access_token"
      pattern: "sessionStorage\\.(get|remove)Item"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/auth.ts"
      via: "initAuth + requestToken on sign-in click"
      pattern: "initAuth|requestToken"
    - from: "frontend/src/lib/api.ts"
      to: "backend/app.py"
      via: "fetch with Authorization: Bearer header"
      pattern: "Authorization.*Bearer"

user_setup:
  - service: google-oauth
    why: "Google OAuth Token Client requires a client ID"
    env_vars:
      - name: VITE_GOOGLE_CLIENT_ID
        source: "Extract from client_secret_*.json at project root (web.client_id field)"
    dashboard_config:
      - task: "Ensure http://localhost:5173 is in Authorized JavaScript origins"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs"
---

<objective>
Google OAuth flow (Token Client), landing page with sign-in, auth-aware API client, app shell with re-auth modal.

Purpose: Wire up the complete auth experience so users can sign in and make authenticated API calls.
Output: Working sign-in flow, token management, re-auth handling, and app shell.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 (backend) -->
From backend/app.py:
```python
# GET /health -> {"status": "ok"}
# CORS: allows http://localhost:5173
```

From backend/auth.py:
```python
async def get_google_user_id(access_token: str) -> str:
    # Returns Google sub claim from access token
```

<!-- From Plan 02 (frontend scaffold) -->
From frontend/src/lib/db.ts:
```typescript
export interface Chat {
  session_id: string;
  title: string;
  created_at: string;
  last_message_at: string;
  indexed_sources: string[];
}
export function openDB(): Promise<IDBDatabase>;
export function getChats(): Promise<Chat[]>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth module, API client, and UI components</name>
  <files>frontend/src/lib/auth.ts, frontend/src/lib/api.ts, frontend/src/lib/api.test.ts, frontend/src/types/google.d.ts, frontend/src/components/landing/LandingPage.tsx, frontend/src/components/app-shell/AppShell.tsx, frontend/src/components/app-shell/ReAuthModal.tsx, frontend/src/App.tsx, frontend/index.html, frontend/.env.example</files>
  <behavior>
    - Test: apiFetch attaches Authorization: Bearer {token} header from sessionStorage
    - Test: apiFetch throws "NO_TOKEN" error when sessionStorage has no token
    - Test: apiFetch throws "TOKEN_EXPIRED" and clears sessionStorage on 401 response
    - Test: apiFetch throws "TOKEN_EXPIRED" and clears sessionStorage on 403 response
  </behavior>
  <action>
    **frontend/src/types/google.d.ts**: Declare types for google.accounts.oauth2 (TokenClient, TokenResponse, initTokenClient). Minimal declarations covering what we use.

    **frontend/index.html**: Add `<script src="https://accounts.google.com/gsi/client" async defer></script>` to head.

    **frontend/.env.example**: VITE_GOOGLE_CLIENT_ID=your-client-id-here, VITE_API_URL=http://localhost:8000

    **frontend/src/lib/auth.ts**:
    - initAuth(onSuccess: (token: string) => void, onError: (err: string) => void): void
    - Uses google.accounts.oauth2.initTokenClient with scope "https://www.googleapis.com/auth/drive.readonly"
    - callback: stores token in sessionStorage("google_access_token"), calls onSuccess
    - error_callback: calls onError with err.type (popup_closed, popup_failed_to_open)
    - Handle BOTH callback error (response.error like access_denied) AND error_callback
    - requestToken(): calls tokenClient.requestAccessToken()
    - Check window.google?.accounts?.oauth2 exists before init; throw clear error if not loaded yet

    **frontend/src/lib/api.ts**:
    - apiFetch(path, options?): fetch wrapper
    - Gets token from sessionStorage, throws "NO_TOKEN" if missing
    - Attaches Authorization: Bearer {token} and Content-Type: application/json
    - On 401/403 response: clears sessionStorage, throws "TOKEN_EXPIRED"
    - API_BASE from import.meta.env.VITE_API_URL

    **frontend/src/lib/api.test.ts**: Tests for apiFetch (mock fetch globally). Write tests FIRST.

    **frontend/src/components/landing/LandingPage.tsx**:
    - shadcn Card centered on page
    - App name/tagline, privacy messaging ("Your files stay in Google Drive. We only read them to answer your questions.")
    - Custom sign-in button (shadcn Button) calling requestToken() on click
    - Per CONTEXT.md decision: inline error when user denies scope, explains why Drive access needed, "Try again" button
    - Handle permission denied (access_denied from callback) and popup closed (from error_callback)

    **frontend/src/components/app-shell/AppShell.tsx**:
    - Post-auth layout: sidebar placeholder (empty, will be built in Phase 5), main content area
    - On mount: skeleton/shimmer for ~500ms while IndexedDB loads (per CONTEXT.md), then render content
    - Load chats from IndexedDB via getChats()
    - Empty state: "Paste a Google Drive link to get started"

    **frontend/src/components/app-shell/ReAuthModal.tsx**:
    - shadcn Dialog (modal overlay)
    - "Your session has expired" message
    - Sign-in button to re-trigger requestToken()
    - Per CONTEXT.md: old chat history visible behind modal (read-only)
    - Shown when TOKEN_EXPIRED error caught

    **frontend/src/App.tsx**:
    - State: token (from sessionStorage on mount), authError, showReAuth
    - If no token: render LandingPage with onSignIn/onError handlers
    - If token: render AppShell
    - Global error boundary catching TOKEN_EXPIRED -> show ReAuthModal
    - initAuth in useEffect on mount (check GIS script loaded)
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && pnpm vitest run src/lib/api.test.ts</automated>
  </verify>
  <done>api.test.ts passes (4+ tests). Auth module exports initAuth/requestToken. Landing page renders sign-in button. App shell shows skeleton then content. ReAuthModal renders on token expiry. App.tsx routes between landing and app shell based on token state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify auth flow end-to-end</name>
  <files>frontend/.env</files>
  <action>
    Human verifies the complete auth flow works in the browser. Before verification, create frontend/.env with VITE_GOOGLE_CLIENT_ID from client_secret_*.json (web.client_id field) and VITE_API_URL=http://localhost:8000. Then run `cd frontend && pnpm dev`.

    Verify:
    1. Visit http://localhost:5173 -- landing page shows with sign-in button and privacy messaging
    2. Click "Sign in with Google" -- Google consent popup appears requesting drive.readonly
    3. After consent: brief skeleton shimmer, then app shell with empty state message
    4. Open DevTools > Application > Session Storage: google_access_token should be present
    5. To test re-auth: manually delete sessionStorage token and trigger API call, or verify ReAuthModal renders
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && pnpm build</automated>
  </verify>
  <done>User confirms: landing page renders, Google sign-in works, token stored in sessionStorage, app shell loads after auth, re-auth modal appears on token expiry.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm vitest run` all green
- `cd frontend && pnpm build` succeeds
- Landing page renders at localhost:5173 without errors
- Sign-in flow completes and stores token in sessionStorage
</verification>

<success_criteria>
- Google Token Client flow works end-to-end (sign in -> token in sessionStorage)
- API client attaches Authorization header and detects 403 for re-auth
- Landing page shows sign-in with privacy messaging and error handling
- App shell shows after auth with skeleton loading
- Re-auth modal appears on token expiry
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
