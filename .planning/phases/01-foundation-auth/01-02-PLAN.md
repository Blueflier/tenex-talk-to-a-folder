---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/vitest.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/app.css
  - frontend/src/lib/utils.ts
  - frontend/src/lib/db.ts
  - frontend/src/lib/db.test.ts
  - frontend/components.json
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03]

must_haves:
  truths:
    - "IndexedDB 'chats' store exists with session_id keyPath and last_message_at index"
    - "IndexedDB 'messages' store exists with autoIncrement, session_id index, and created_at index"
    - "Chat history loads from IndexedDB with no server calls"
    - "Vite dev server runs on localhost:5173 with React + TypeScript"
  artifacts:
    - path: "frontend/src/lib/db.ts"
      provides: "IndexedDB open, CRUD for chats and messages"
      exports: ["openDB", "getChats", "getMessages", "saveChat", "saveMessage"]
    - path: "frontend/src/lib/db.test.ts"
      provides: "Tests for IndexedDB operations"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with Tailwind v4 plugin"
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration with jsdom"
  key_links:
    - from: "frontend/src/lib/db.ts"
      to: "IndexedDB"
      via: "indexedDB.open('talk-to-a-folder', 1)"
      pattern: "indexedDB\\.open"
---

<objective>
Frontend scaffold with Vite + React + TypeScript + shadcn/ui + Tailwind v4, plus IndexedDB persistence layer with full CRUD.

Purpose: Establish the frontend foundation and local persistence so auth and UI plans can build on it.
Output: Running Vite dev server, shadcn/ui initialized, IndexedDB module with passing tests.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + shadcn/ui project</name>
  <files>frontend/ (entire scaffold)</files>
  <action>
    Run the following commands from project root:

    1. `pnpm create vite@latest frontend -- --template react-ts`
    2. `cd frontend && pnpm install`
    3. `pnpm add tailwindcss @tailwindcss/vite`
    4. `pnpm add -D @types/node vitest @testing-library/react @testing-library/jest-dom jsdom fake-indexeddb`

    Update vite.config.ts:
    - Add tailwindcss() to plugins
    - Add resolve.alias for "@" pointing to src/

    Create vitest.config.ts:
    - environment: "jsdom"
    - globals: true
    - setupFiles: [] (add if needed)

    Update frontend/src/index.css to use Tailwind v4:
    ```css
    @import "tailwindcss";
    ```

    Run `pnpm dlx shadcn@latest init` (accept defaults, use New York style, zinc base color).
    Run `pnpm dlx shadcn@latest add button card dialog` to install needed components.

    Clean up default App.tsx to render a minimal placeholder div.
    Verify `pnpm dev` starts without errors.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && pnpm dev --port 5173 &>/dev/null & sleep 3 && curl -s http://localhost:5173 | head -5 && kill %1 2>/dev/null; echo "OK"</automated>
  </verify>
  <done>Vite dev server starts on 5173. shadcn/ui components available. Tailwind v4 active. Vitest configured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IndexedDB persistence layer with CRUD operations</name>
  <files>frontend/src/lib/db.ts, frontend/src/lib/db.test.ts</files>
  <behavior>
    - Test: openDB creates "chats" store with session_id keyPath
    - Test: openDB creates "messages" store with autoIncrement
    - Test: "chats" store has last_message_at index
    - Test: "messages" store has session_id index and created_at index
    - Test: saveChat + getChats roundtrip returns saved chat
    - Test: saveMessage + getMessages(session_id) returns messages for that session only
    - Test: getChats returns chats sorted by last_message_at descending
  </behavior>
  <action>
    Create frontend/src/lib/db.ts following the exact IndexedDB pattern from RESEARCH.md:

    - DB_NAME = "talk-to-a-folder", DB_VERSION = 1
    - openDB(): Promise<IDBDatabase> -- creates "chats" store (keyPath: session_id, index: last_message_at) and "messages" store (autoIncrement, indexes: session_id, created_at)
    - Chat type: { session_id: string, title: string, created_at: string, last_message_at: string, indexed_sources: string[] }
    - Message type: { session_id: string, role: "user" | "assistant", content: string, citations: any[], created_at: string }
    - saveChat(chat: Chat): Promise<void>
    - getChats(): Promise<Chat[]> -- sorted by last_message_at descending
    - saveMessage(msg: Message): Promise<void>
    - getMessages(sessionId: string): Promise<Message[]> -- filtered by session_id index

    Export all types and functions.

    Create frontend/src/lib/db.test.ts:
    - Import "fake-indexeddb/auto" at top to polyfill IndexedDB
    - Write tests FIRST (RED), then implement (GREEN)
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && pnpm vitest run src/lib/db.test.ts</automated>
  </verify>
  <done>All 7+ IndexedDB tests pass. db.ts exports openDB, getChats, getMessages, saveChat, saveMessage, Chat type, Message type.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm vitest run` all green
- `cd frontend && pnpm build` succeeds without errors
- IndexedDB module exports all required functions and types
</verification>

<success_criteria>
- Vite + React + TypeScript project runs on localhost:5173
- shadcn/ui initialized with button, card, dialog components
- Tailwind v4 active via @tailwindcss/vite plugin
- IndexedDB "chats" and "messages" stores with correct schemas
- CRUD operations (save/get chats, save/get messages) working and tested
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
