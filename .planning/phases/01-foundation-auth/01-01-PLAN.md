---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app.py
  - backend/auth.py
  - backend/config.py
  - backend/requirements.txt
  - backend/tests/test_auth.py
  - backend/tests/test_config.py
  - backend/tests/test_cors.py
  - backend/tests/conftest.py
autonomous: true
requirements: [INFR-01, INFR-02, INFR-04, AUTH-04]

must_haves:
  truths:
    - "FastAPI app defines Modal function with Volume at /data and secrets attached"
    - "CORS allows localhost:5173 and rejects unlisted origins"
    - "Model config returns DeepSeek client by default and OpenAI client when switched"
    - "Backend extracts Google sub claim from access token via userinfo endpoint"
  artifacts:
    - path: "backend/app.py"
      provides: "Modal app + FastAPI with CORS, Volume, secrets, health endpoint"
      exports: ["web_app", "fastapi_app"]
    - path: "backend/auth.py"
      provides: "Google userinfo verification returning sub claim"
      exports: ["get_google_user_id"]
    - path: "backend/config.py"
      provides: "Model strategy config with DeepSeek default"
      exports: ["get_llm_client", "MODEL_CONFIGS", "ACTIVE_MODEL"]
    - path: "backend/requirements.txt"
      provides: "Python dependencies"
  key_links:
    - from: "backend/app.py"
      to: "backend/auth.py"
      via: "import get_google_user_id"
      pattern: "from auth import get_google_user_id"
    - from: "backend/app.py"
      to: "backend/config.py"
      via: "import get_llm_client"
      pattern: "from config import"
---

<objective>
Backend scaffold: Modal + FastAPI app with Volume mount, CORS, Google auth verification, and model strategy config.

Purpose: Establish the entire backend foundation so frontend can make authenticated API calls.
Output: Deployable Modal app with health endpoint, auth middleware, model config, and passing tests.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend scaffold -- Modal app, CORS, auth, model config</name>
  <files>backend/app.py, backend/auth.py, backend/config.py, backend/requirements.txt</files>
  <behavior>
    - Test: CORS allows localhost:5173 origin (returns correct Access-Control-Allow-Origin)
    - Test: CORS rejects unlisted origin (no CORS headers)
    - Test: get_google_user_id returns sub claim when Google API returns 200
    - Test: get_google_user_id raises HTTPException(401) when Google API returns non-200
    - Test: get_llm_client with ACTIVE_MODEL="deepseek" returns client with deepseek base_url
    - Test: get_llm_client with ACTIVE_MODEL="openai" returns client with openai base_url
    - Test: health endpoint returns {"status": "ok"}
  </behavior>
  <action>
    Create backend/ directory with four files:

    **backend/requirements.txt**: modal, fastapi, aiohttp, openai, numpy, pytest, pytest-asyncio, httpx

    **backend/config.py**: MODEL_CONFIGS dict with "deepseek" (deepseek-chat, api.deepseek.com, DEEPSEEK_API_KEY) and "openai" (gpt-4o-mini, api.openai.com/v1, OPENAI_API_KEY). ACTIVE_MODEL from env with "deepseek" default. get_llm_client() returns (AsyncOpenAI client, model_name) tuple. VOLUME_PATH = Path("/data") constant.

    **backend/auth.py**: get_google_user_id(access_token: str) -> str. Uses aiohttp to call googleapis.com/oauth2/v3/userinfo with Bearer token. Returns info["sub"] on 200. Raises HTTPException(401, "Invalid access token") otherwise. Follow exact pattern from RESEARCH.md.

    **backend/app.py**: Modal app "talk-to-a-folder". Volume from_name "talk-to-a-folder-data" (create_if_missing=True). FastAPI web_app with CORSMiddleware: allow_origins=["http://localhost:5173"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"]. Health endpoint GET /health returning {"status": "ok"}. Modal function with volumes={"/data": volume}, image with pip_install from requirements, secrets=[openai-secret, deepseek-secret]. @modal.asgi_app() decorator.

    Write tests FIRST (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder && pip install -q pytest pytest-asyncio httpx fastapi aiohttp openai 2>/dev/null && cd backend && python -m pytest tests/ -x -v</automated>
  </verify>
  <done>All 7+ tests pass. backend/app.py, auth.py, config.py, requirements.txt exist with correct exports.</done>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/ -x -v` all green
- `python -c "from backend.config import get_llm_client, MODEL_CONFIGS; print('OK')"` succeeds
- `python -c "from backend.auth import get_google_user_id; print('OK')"` succeeds
</verification>

<success_criteria>
- FastAPI app with CORS, health endpoint, Volume mount, and secrets defined
- Auth module verifies Google access tokens via userinfo endpoint
- Model config supports DeepSeek (default) and OpenAI with client swap
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
