---
phase: 03-retrieval-chat
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - frontend/src/components/chat/ChatView.tsx
  - frontend/src/components/chat/EmptyState.tsx
  - frontend/src/lib/db.ts
  - frontend/src/lib/suggestions.ts
  - frontend/tests/citations.test.ts
  - frontend/tests/db.test.ts
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-06, PERS-04]

must_haves:
  truths:
    - "Citation metadata is frozen on each message and persists in IndexedDB after stream completes"
    - "Old chat messages load from IndexedDB and display without requiring authentication"
    - "New message sends require a valid access token; missing token shows re-auth prompt"
    - "After indexing, user sees file count header and 3-4 clickable suggestion cards"
    - "Clicking a suggestion auto-fills the input; suggestions disappear after first message"
  artifacts:
    - path: "frontend/src/components/chat/EmptyState.tsx"
      provides: "Pre-chat empty state with suggestion cards"
    - path: "frontend/src/lib/suggestions.ts"
      provides: "Suggestion generation from file names"
      exports: ["generateSuggestions"]
    - path: "frontend/src/lib/db.ts"
      provides: "IndexedDB operations for messages with citations"
    - path: "frontend/tests/citations.test.ts"
      provides: "Tests for citation formatting and persistence"
    - path: "frontend/tests/db.test.ts"
      provides: "Tests for message loading without auth"
  key_links:
    - from: "frontend/src/components/chat/ChatView.tsx"
      to: "frontend/src/lib/db.ts"
      via: "saveMessage with citations, loadMessages"
      pattern: "saveMessage|loadMessages"
    - from: "frontend/src/components/chat/ChatView.tsx"
      to: "sessionStorage"
      via: "auth guard before sendMessage"
      pattern: "sessionStorage.*google_access_token"
    - from: "frontend/src/components/chat/EmptyState.tsx"
      to: "frontend/src/lib/suggestions.ts"
      via: "generateSuggestions(indexedSources)"
      pattern: "generateSuggestions"
---

<objective>
Wire citation persistence to IndexedDB, implement auth guard for new messages, and add pre-chat empty state with suggestions.

Purpose: Complete the chat experience by persisting citation data, enabling read-only old chats, and providing onboarding UX.
Output: Fully integrated chat flow with frozen citations, auth-aware message sending, and empty state suggestions.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-retrieval-chat/03-CONTEXT.md
@.planning/phases/03-retrieval-chat/03-RESEARCH.md
@.planning/phases/03-retrieval-chat/03-01-SUMMARY.md
@.planning/phases/03-retrieval-chat/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Citation persistence + auth guard + IndexedDB wiring</name>
  <files>frontend/src/lib/db.ts, frontend/src/components/chat/ChatView.tsx, frontend/tests/citations.test.ts, frontend/tests/db.test.ts</files>
  <behavior>
    - formatCitationLabel("report.pdf", page_number=7) returns "report.pdf, p.7"
    - formatCitationLabel("data.csv", row_number=12) returns "data.csv, row 12"
    - formatCitationLabel("deck.pptx", slide_index=3) returns "deck.pptx, slide 3"
    - saveMessage stores citations array as frozen snapshot on the message record
    - loadMessages(sessionId) returns messages with citations from IndexedDB (no server call)
    - loadMessages works without any auth token in sessionStorage (PERS-04)
    - handleSend blocks and shows re-auth prompt when sessionStorage has no google_access_token
  </behavior>
  <action>
    **Update `frontend/src/lib/db.ts`** (from Phase 1):
    - Add `citations` field to the message record type: `citations?: Citation[]`
    - `saveMessage(sessionId, role, content, citations?)` — stores message with frozen citations array to IndexedDB "messages" store
    - `loadMessages(sessionId)` — loads all messages for session from IndexedDB, sorted by created_at. No auth check, no server call (PERS-04).
    - Ensure message record includes: session_id (indexed), role, content, citations, created_at

    **Update `frontend/src/components/chat/ChatView.tsx`:**
    - On mount: call `loadMessages(sessionId)` to populate message history from IndexedDB
    - In useStream `onDone` callback: call `saveMessage(sessionId, "assistant", fullContent, citations)` with the citations received from the citations event
    - In useStream `onCitations` callback: store citations in local state, then pass to saveMessage when done
    - Also save user message on send: `saveMessage(sessionId, "user", query)`
    - Auth guard in handleSend: check `sessionStorage.getItem("google_access_token")`. If null, show re-auth state (set `needsAuth=true` state, render Phase 1's auth banner/modal). Do NOT call useStream.sendMessage without token.
    - For no_results: save assistant message with content = the muted message text, no citations

    **Create `frontend/tests/citations.test.ts`:**
    - Test formatCitationLabel for PDF, Sheet, Slides, plain file
    - Test that citation objects match expected schema

    **Create `frontend/tests/db.test.ts`:**
    - Mock IndexedDB (use fake-indexeddb or manual mock)
    - Test saveMessage stores citations on record
    - Test loadMessages returns messages without requiring any auth state
    - Test loadMessages returns messages sorted by created_at
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && pnpm test -- --run</automated>
  </verify>
  <done>Citations stored as frozen snapshots on IndexedDB messages; old messages load without auth; auth guard blocks new sends when token missing; all frontend tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Empty state with suggestion cards</name>
  <files>frontend/src/components/chat/EmptyState.tsx, frontend/src/lib/suggestions.ts, frontend/src/components/chat/ChatView.tsx</files>
  <action>
    **Create `frontend/src/lib/suggestions.ts`:**
    - `generateSuggestions(indexedSources)` — takes array of indexed source objects (with file_list containing name fields)
    - Templates: "Summarize {name}", "What are the key points in {name}?", "What does {name} say about..."
    - Pick up to 4 suggestions from different files, cycling through templates
    - No LLM call — purely template-based per locked decision

    **Create `frontend/src/components/chat/EmptyState.tsx`:**
    - Props: indexedSources, onSuggestionClick(query: string)
    - Header: "{N} files indexed" with file count from indexedSources
    - 3-4 suggestion cards below in a grid (2 cols on desktop, 1 on mobile)
    - Each card shows the suggestion text, styled as clickable card (border, hover state, rounded corners)
    - onClick calls onSuggestionClick with the suggestion text

    **Update `frontend/src/components/chat/ChatView.tsx`:**
    - Show EmptyState when messages array is empty and indexing is complete
    - onSuggestionClick: set input value to suggestion text (auto-fill, don't auto-send)
    - After first message is sent (messages.length > 0), EmptyState no longer renders
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>EmptyState shows file count + suggestion cards; clicking a suggestion fills the input; suggestions disappear after first message; no LLM call for suggestions</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test -- --run` — all frontend tests pass
- `cd frontend && npx tsc --noEmit` — no type errors
- Messages with citations persist to IndexedDB and reload on page refresh
- Old chats load without auth token present
- Empty state shows suggestions after indexing
</verification>

<success_criteria>
- Citation metadata frozen on each IndexedDB message record at answer time
- Old chat messages load from IndexedDB without authentication
- Auth guard prevents new message sends without valid token
- Empty state displays file count and clickable suggestion cards
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-retrieval-chat/03-03-SUMMARY.md`
</output>
