---
phase: 05-multi-session-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/chat.py
  - backend/storage.py
  - backend/index.py
  - backend/tests/test_chat_endpoint.py
  - backend/tests/test_storage.py
autonomous: true
requirements: [INFR-03, INDX-14]

must_haves:
  truths:
    - "/chat returns 429 after 10 requests in 60 seconds for the same session"
    - "Pasting a second Drive link into an existing session appends embeddings instead of overwriting"
  artifacts:
    - path: "backend/chat.py"
      provides: "Rate limiting on /chat endpoint"
      contains: "_check_rate_limit"
    - path: "backend/storage.py"
      provides: "append_session function for multi-link"
      exports: ["save_session", "load_session", "append_session"]
    - path: "backend/index.py"
      provides: "Index endpoint uses append_session when session data exists"
  key_links:
    - from: "backend/chat.py"
      to: "_check_rate_limit"
      via: "called before processing in chat_endpoint"
      pattern: "_check_rate_limit\\(session_id\\)"
    - from: "backend/index.py"
      to: "backend/storage.py"
      via: "append_session when existing data found"
      pattern: "append_session"
---

<objective>
Add backend rate limiting on /chat (10 req/min per session) and multi-link append logic so pasting additional Drive links concatenates embeddings to existing session data.

Purpose: Backend prerequisites for Phase 5 frontend features (rate limit feedback, multi-link sessions).
Output: Updated chat.py with rate limiter, storage.py with append_session, index.py using append when session exists.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/chat.py:
```python
@router.post("/chat")
async def chat_endpoint(request: Request, authorization: str = Header(None)):
    # body: { session_id, query, file_list }
    # Returns StreamingResponse with SSE events
```

From backend/storage.py:
```python
def save_session(user_id, session_id, embeddings, chunks, volume, base_path=None) -> None
def load_session(user_id, session_id, base_path=None) -> tuple[np.ndarray, list[dict]]
```

From backend/index.py:
```python
# Line 208: save_session(user_id, session_id, embeddings, all_chunks, volume)
# Currently always overwrites. Needs to detect existing and append.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rate limiter on /chat + append_session in storage.py</name>
  <files>backend/chat.py, backend/storage.py, backend/index.py, backend/tests/test_chat_endpoint.py, backend/tests/test_storage.py</files>
  <behavior>
    - Rate limit: 11th request within 60s for same session_id returns 429
    - Rate limit: requests to different session_ids are independent
    - Rate limit: after 60s window expires, requests succeed again
    - append_session: when session files exist, new embeddings are concatenated to existing .npy and new chunks appended to existing .json
    - append_session: when session files don't exist, behaves like save_session
    - index endpoint: detects existing session data and calls append logic instead of overwrite
  </behavior>
  <action>
1. In backend/chat.py, add module-level `_rate_limits: dict[str, list[float]] = defaultdict(list)` and `_check_rate_limit(session_id: str) -> bool` function. Uses sliding window: prune timestamps older than 60s, return True if >= 10 remaining. In `chat_endpoint`, call `_check_rate_limit(session_id)` before processing. If True, raise `HTTPException(status_code=429, detail="Rate limit exceeded. Try again in a minute.")`.

2. In backend/storage.py, add `append_session(user_id, session_id, new_embeddings, new_chunks, volume, base_path=None)` function. Check if session files exist: if yes, load existing with `load_session`, concatenate `np.concatenate([existing_emb, new_embeddings])` and `existing_chunks + new_chunks`, then call `save_session` with combined data. If no existing files, call `save_session` directly with new data.

3. In backend/index.py line ~208, replace `save_session(...)` with `append_session(...)`. Import `append_session` from `backend.storage`.

4. Extend backend/tests/test_chat_endpoint.py with rate limit tests. Extend backend/tests/test_storage.py with append_session tests using base_path for temp dir isolation.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && python -m pytest tests/test_chat_endpoint.py tests/test_storage.py -x -v</automated>
  </verify>
  <done>
    - /chat returns 429 after 10 requests per session per minute
    - append_session concatenates embeddings and chunks when session exists
    - index.py uses append_session instead of save_session
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/ -x` passes
- Rate limit behavior verified in test
- Append logic verified with fixture data
</verification>

<success_criteria>
- /chat endpoint returns 429 status after 10 requests in 60s window for same session
- Second indexing call to same session appends rather than overwrites embeddings/chunks
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-session-polish/05-01-SUMMARY.md`
</output>
