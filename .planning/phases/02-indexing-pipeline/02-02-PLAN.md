---
phase: 02-indexing-pipeline
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/embedding.py
  - backend/storage.py
  - backend/tests/test_embedding.py
  - backend/tests/test_storage.py
autonomous: true
requirements: [INDX-10, INDX-11, INDX-12]

must_haves:
  truths:
    - "Chunks are embedded in batches of 100 via OpenAI text-embedding-3-small"
    - "Embedding failures retry up to 3 times with exponential backoff"
    - "Progress callback fires after each batch"
    - "Embeddings saved as .npy and chunks as .json namespaced by user_id/session_id"
    - "volume.commit() is called after every write"
  artifacts:
    - path: "backend/embedding.py"
      provides: "Batch embedding with progress and retry"
      exports: ["embed_chunks", "BATCH_SIZE", "EMBED_MODEL"]
    - path: "backend/storage.py"
      provides: "Modal Volume save/load for session data"
      exports: ["save_session", "load_session"]
    - path: "backend/tests/test_embedding.py"
      provides: "Embedding tests with mocked OpenAI client"
    - path: "backend/tests/test_storage.py"
      provides: "Storage tests with mocked volume"
  key_links:
    - from: "backend/embedding.py"
      to: "OpenAI API"
      via: "AsyncOpenAI.embeddings.create"
      pattern: "client\\.embeddings\\.create"
    - from: "backend/storage.py"
      to: "Modal Volume"
      via: "np.save + json.dump + volume.commit()"
      pattern: "volume\\.commit"
---

<objective>
Batch embedding and Modal Volume storage -- the persistence layer for indexed content.

Purpose: Embed chunks via OpenAI and persist embeddings + metadata to Modal Volume.
Output: backend/embedding.py, backend/storage.py with full test coverage.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-indexing-pipeline/02-RESEARCH.md

<interfaces>
<!-- Chunk dict format produced by chunking.py (from Plan 01, same wave -- use contract) -->
```python
# Chunk dicts have these shapes depending on type:
# PDF:    {"text": str, "source": str, "page": int, "chunk_index": int}
# Sheet:  {"text": str, "source": str, "row": int, "chunk_index": int}
# Slides: {"text": str, "source": str, "slide": int, "chunk_index": int}
# Text:   {"text": str, "source": str, "chunk_index": int}
# All have "text" and "source" keys at minimum.
```

From backend/app.py:
```python
volume = modal.Volume.from_name("talk-to-a-folder-data", create_if_missing=True)
# Mounted at /data
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Batch embedding with progress and retry</name>
  <files>backend/embedding.py, backend/tests/test_embedding.py</files>
  <behavior>
    - embed_chunks sends texts in batches of 100 to OpenAI
    - embed_chunks returns numpy array of shape (n_chunks, 1536) dtype float32
    - on_progress callback receives (processed_count, total_count) after each batch
    - 150 chunks produce 2 batches (100 + 50)
    - Transient API error retries up to 3 times with exponential backoff
    - Permanent failure after 3 retries raises the exception
    - Empty chunks list returns empty (0, 1536) array
  </behavior>
  <action>
    RED: Write tests in backend/tests/test_embedding.py. Mock AsyncOpenAI client -- create a mock that returns fake embedding vectors (list of 1536 floats). Test batch splitting (150 chunks = 2 calls), progress callback, retry on exception, empty input.

    GREEN: Implement backend/embedding.py following 02-RESEARCH.md Pattern 4 exactly. BATCH_SIZE=100, EMBED_MODEL="text-embedding-3-small". async def embed_chunks(client, chunks, on_progress=None, max_retries=3) -> np.ndarray.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && pytest tests/test_embedding.py -x -v</automated>
  </verify>
  <done>embed_chunks batches correctly, returns proper numpy shape, progress callback works, retry logic handles transient failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modal Volume session storage</name>
  <files>backend/storage.py, backend/tests/test_storage.py</files>
  <behavior>
    - save_session creates /data/{user_id}/{session_id}_embeddings.npy
    - save_session creates /data/{user_id}/{session_id}_chunks.json
    - save_session calls volume.commit() after each write (2 times total)
    - save_session creates user directory if it doesn't exist
    - load_session returns (embeddings_array, chunks_list) tuple
    - load_session raises FileNotFoundError if session doesn't exist
    - Saved embeddings round-trip correctly (save then load)
  </behavior>
  <action>
    RED: Write tests in backend/tests/test_storage.py. Use tmp_path for filesystem and a mock volume object (MagicMock with commit method). Verify file paths, commit call count, round-trip data integrity.

    GREEN: Implement backend/storage.py following 02-RESEARCH.md Pattern 5. VOLUME_PATH = Path("/data"). Functions: save_session(user_id, session_id, embeddings, chunks, volume), load_session(user_id, session_id). In tests, override VOLUME_PATH to tmp_path.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && pytest tests/test_storage.py -x -v</automated>
  </verify>
  <done>save_session writes .npy + .json with correct paths. volume.commit() called twice. load_session reads back identical data. Missing session raises FileNotFoundError.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && pytest tests/test_embedding.py tests/test_storage.py -v
```
All tests green.
</verification>

<success_criteria>
- backend/embedding.py batches in groups of 100, retries 3x, fires progress callback
- backend/storage.py writes to /data/{user_id}/{session_id}_*.npy and _*.json
- volume.commit() verified called after every write
- Round-trip save/load produces identical data
</success_criteria>

<output>
After completion, create `.planning/phases/02-indexing-pipeline/02-02-SUMMARY.md`
</output>
