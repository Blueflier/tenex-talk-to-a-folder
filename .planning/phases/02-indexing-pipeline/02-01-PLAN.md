---
phase: 02-indexing-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/drive.py
  - backend/chunking.py
  - backend/tests/test_drive.py
  - backend/tests/test_chunking.py
  - backend/tests/fixtures/sample.pdf
autonomous: true
requirements: [INDX-01, INDX-02, INDX-03, INDX-04, INDX-05, INDX-06, INDX-07, INDX-08]

must_haves:
  truths:
    - "Drive URL string is parsed to a file or folder ID"
    - "Folder ID resolves to a list of files with id, name, mimeType, size"
    - "Google Docs export as plain text"
    - "PDFs extract page-by-page and chunk with recursive splitter"
    - "Sheets export as CSV and chunk row-level with headers prepended"
    - "Slides export as plain text and split per slide on double newline"
    - "TXT/MD files chunk with recursive character splitter (1200/150)"
    - "Unsupported mimeTypes are detected and skipped with a reason"
  artifacts:
    - path: "backend/drive.py"
      provides: "Drive link parsing, file resolution, folder listing, file export"
      exports: ["extract_drive_id", "resolve_drive_link", "list_folder_files", "export_file", "SUPPORTED_MIME_TYPES", "SKIP_REASONS"]
    - path: "backend/chunking.py"
      provides: "Type-specific chunking strategies"
      exports: ["recursive_chunk", "chunk_pdf", "chunk_sheet", "chunk_slides", "chunk_text"]
    - path: "backend/tests/test_drive.py"
      provides: "Tests for drive module"
    - path: "backend/tests/test_chunking.py"
      provides: "Tests for chunking module"
  key_links:
    - from: "backend/drive.py"
      to: "Google Drive API v3"
      via: "aiohttp with Bearer token"
      pattern: "googleapis.com/drive/v3"
    - from: "backend/chunking.py"
      to: "pymupdf"
      via: "pymupdf.open(stream=bytes)"
      pattern: "pymupdf\\.open"
---

<objective>
Drive link resolution, file export, and type-specific chunking -- the core data extraction layer.

Purpose: Build the modules that turn a Drive URL into chunked text ready for embedding.
Output: backend/drive.py, backend/chunking.py with full test coverage.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-indexing-pipeline/02-RESEARCH.md
@.planning/phases/02-indexing-pipeline/02-CONTEXT.md

<interfaces>
<!-- Existing backend interfaces this plan needs -->

From backend/auth.py:
```python
async def get_google_user_id(access_token: str) -> str:
    """Verify Google access token and return the user's sub claim."""
```

From backend/app.py:
```python
app = modal.App("talk-to-a-folder")
volume = modal.Volume.from_name("talk-to-a-folder-data", create_if_missing=True)
web_app = FastAPI()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive link resolution and file export module</name>
  <files>backend/drive.py, backend/tests/test_drive.py</files>
  <behavior>
    - extract_drive_id parses folder URLs (/folders/ID) to ID
    - extract_drive_id parses file URLs (/d/ID) to ID
    - extract_drive_id parses open?id=ID to ID
    - extract_drive_id returns None for invalid URLs
    - resolve_drive_link returns {id, name, mimeType, size} for valid file (mock aiohttp)
    - resolve_drive_link raises ValueError on 404, PermissionError on 403
    - list_folder_files returns list of file dicts with pagination (mock aiohttp, test 2 pages)
    - export_file uses /export endpoint for Google Workspace types, ?alt=media for binary (mock aiohttp)
    - SUPPORTED_MIME_TYPES contains all 6 supported types
    - is_supported returns False for image/*, video/*, application/zip with reason from SKIP_REASONS
  </behavior>
  <action>
    RED: Write tests in backend/tests/test_drive.py covering all behaviors above. Mock aiohttp.ClientSession using pytest-asyncio. Use aioresponses or manual mock. Tests must fail.

    GREEN: Implement backend/drive.py following the exact patterns from 02-RESEARCH.md Pattern 1 and Pattern 2. Functions: extract_drive_id (regex), resolve_drive_link (aiohttp GET files/{id}), list_folder_files (paginated files.list), export_file (export vs alt=media), classify_file (supported check + skip reason). Use DRIVE_FOLDER_PATTERN, DRIVE_FILE_PATTERN, DRIVE_URL_PATTERN regexes from research.

    Add `aioresponses` to requirements.txt for mocking.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && pip install -r requirements.txt -q && pytest tests/test_drive.py -x -v</automated>
  </verify>
  <done>All drive.py tests green. extract_drive_id handles folder/file/open URLs. resolve_drive_link, list_folder_files, export_file work with mocked HTTP. Unsupported mimeTypes classified correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type-specific chunking strategies</name>
  <files>backend/chunking.py, backend/tests/test_chunking.py, backend/tests/fixtures/sample.pdf</files>
  <behavior>
    - recursive_chunk splits text at 1200 chars with 150 overlap
    - recursive_chunk produces overlapping chunks (end of chunk N overlaps start of chunk N+1)
    - recursive_chunk skips empty/whitespace-only chunks
    - chunk_pdf extracts page-by-page, each chunk has source, page, chunk_index
    - chunk_pdf returns empty list for scanned PDF (no text)
    - chunk_sheet splits CSV row-level, prepends header to each row
    - chunk_sheet returns empty list for header-only CSV
    - chunk_slides splits on double newline, each chunk has source, slide number
    - chunk_slides filters empty slides
    - chunk_text wraps recursive_chunk with source and chunk_index metadata
  </behavior>
  <action>
    First: Create backend/tests/fixtures/sample.pdf -- a small 2-page PDF with known text content using reportlab or pymupdf itself (generate programmatically in a conftest fixture, or create a minimal valid PDF bytes literal).

    RED: Write tests in backend/tests/test_chunking.py for all behaviors. Use the sample PDF fixture for chunk_pdf tests. Test edge cases: empty text, single-row CSV, slides with empty slides.

    GREEN: Implement backend/chunking.py following 02-RESEARCH.md Pattern 3 exactly. Functions: recursive_chunk(text, max_chars=1200, overlap=150), chunk_pdf(pdf_bytes, file_name), chunk_sheet(csv_text, file_name), chunk_slides(text, file_name), chunk_text(text, file_name).

    Add `pymupdf` to requirements.txt. Pin `fastapi>=0.135.0` while editing requirements.txt.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && pip install pymupdf -q && pytest tests/test_chunking.py -x -v</automated>
  </verify>
  <done>All chunking tests green. recursive_chunk handles 1200/150 params. chunk_pdf extracts pages with metadata. chunk_sheet prepends headers. chunk_slides splits on double newline. Edge cases (empty text, scanned PDF, header-only CSV) handled.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/josephhartono/src/tenex-talk-to-a-folder/backend && pytest tests/test_drive.py tests/test_chunking.py -v
```
All tests green. No import errors. Both modules importable independently.
</verification>

<success_criteria>
- backend/drive.py exports extract_drive_id, resolve_drive_link, list_folder_files, export_file, SUPPORTED_MIME_TYPES
- backend/chunking.py exports recursive_chunk, chunk_pdf, chunk_sheet, chunk_slides, chunk_text
- All chunk dicts include source, and type-specific metadata (page, row, slide, chunk_index)
- Tests cover happy path + edge cases for all functions
- pymupdf added to requirements.txt
</success_criteria>

<output>
After completion, create `.planning/phases/02-indexing-pipeline/02-01-SUMMARY.md`
</output>
