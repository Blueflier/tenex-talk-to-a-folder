---
phase: 06-eval-harness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - eval/__init__.py
  - eval/scoring.py
  - eval/classify.py
  - eval/dataset.py
  - eval/tests/__init__.py
  - eval/tests/conftest.py
  - eval/tests/test_scoring.py
  - eval/tests/test_classify.py
  - eval/tests/test_dataset.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03]

must_haves:
  truths:
    - "Token F1 correctly scores extractive/abstractive answers"
    - "Exact match correctly scores yes/no/unanswerable answers"
    - "Max-across-annotators scoring picks highest F1"
    - "CRAWL_MISS detected when gold answer absent from all chunks"
    - "RETRIEVAL_MISS detected when gold answer not in retrieved set"
    - "SYNTHESIS_FAIL classified when answer was retrievable but LLM failed"
    - "QASPER papers selected by question density with seed reproducibility"
    - "Gold answers extracted from nested QASPER annotator structure"
  artifacts:
    - path: "eval/scoring.py"
      provides: "normalize_answer, token_f1, score_answer"
      exports: ["normalize_answer", "token_f1", "score_answer"]
    - path: "eval/classify.py"
      provides: "classify_failure heuristic"
      exports: ["classify_failure"]
    - path: "eval/dataset.py"
      provides: "load_qasper_papers, extract_gold_answers"
      exports: ["load_qasper_papers", "extract_gold_answers"]
    - path: "eval/tests/test_scoring.py"
      provides: "Unit tests for scoring module"
    - path: "eval/tests/test_classify.py"
      provides: "Unit tests for classification module"
    - path: "eval/tests/test_dataset.py"
      provides: "Unit tests for dataset module"
  key_links:
    - from: "eval/scoring.py"
      to: "eval/classify.py"
      via: "classify_failure uses scoring internally to check if answer was correct"
      pattern: "score_answer.*gold_answers"
---

<objective>
Build and test the three core eval logic modules: scoring (token F1 + exact match), diagnostic classification (CRAWL_MISS / RETRIEVAL_MISS / SYNTHESIS_FAIL), and QASPER dataset loading with paper selection.

Purpose: These are the pure-logic foundations the eval pipeline depends on. TDD ensures correctness before integration.
Output: eval/scoring.py, eval/classify.py, eval/dataset.py with full test coverage.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-eval-harness/06-CONTEXT.md
@.planning/phases/06-eval-harness/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scoring module — normalize_answer, token_f1, score_answer</name>
  <files>eval/__init__.py, eval/scoring.py, eval/tests/__init__.py, eval/tests/conftest.py, eval/tests/test_scoring.py</files>
  <behavior>
    - normalize_answer("The Cat.") -> "cat"
    - normalize_answer("  a  an  the  ") -> "" (all articles removed)
    - token_f1("the cat sat", "the cat sat") -> 1.0
    - token_f1("the cat sat", "the dog sat") -> ~0.5 (partial overlap)
    - token_f1("", "") -> 1.0
    - token_f1("hello", "") -> 0.0
    - score_answer("Yes", [{"type": "yes_no", "text": "Yes"}]) -> 1.0
    - score_answer("No", [{"type": "yes_no", "text": "Yes"}]) -> 0.0
    - score_answer("Unanswerable", [{"type": "unanswerable", "text": "Unanswerable"}]) -> 1.0
    - score_answer("some text", [annotator1, annotator2]) -> max F1 across annotators
    - score_answer with mixed types picks best score
  </behavior>
  <action>
Create eval/__init__.py and eval/tests/__init__.py as empty files. Create eval/tests/conftest.py with shared fixtures (sample chunks list, sample embeddings numpy array, sample QASPER-like data dict).

RED: Write eval/tests/test_scoring.py with tests for all behaviors above. Run tests -- they must fail (no implementation).

GREEN: Implement eval/scoring.py with:
- normalize_answer(text: str) -> str: lowercase, remove articles (a/an/the), remove punctuation, collapse whitespace. Use SQuAD-style approach from 06-RESEARCH.md Pattern 3.
- token_f1(prediction: str, reference: str) -> float: tokenize normalized strings, compute precision/recall/F1 via Counter intersection. Handle empty edge cases.
- score_answer(prediction: str, gold_answers: list[dict]) -> float: for each gold answer, use exact match for yes_no/unanswerable types (compare normalized), token_f1 for extractive/abstractive. Return max score across all annotators. Return 0.0 if gold_answers is empty.

Run tests -- they must pass.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder && python -m pytest eval/tests/test_scoring.py -x -q</automated>
  </verify>
  <done>All scoring tests pass. normalize_answer handles articles/punctuation/whitespace. token_f1 computes correct F1. score_answer handles all QASPER answer types with max-across-annotators.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classification + dataset modules</name>
  <files>eval/classify.py, eval/dataset.py, eval/tests/test_classify.py, eval/tests/test_dataset.py</files>
  <behavior>
    - classify_failure returns "CRAWL_MISS" when gold text not found in any chunk
    - classify_failure returns "RETRIEVAL_MISS" when gold embedding has low cosine sim to retrieved embeddings
    - classify_failure returns "SYNTHESIS_FAIL" when gold text IS in chunks and retrieved embeddings are similar
    - classify_failure returns "RETRIEVAL_MISS" when retrieved_embeddings is empty
    - load_qasper_papers(n=5, seed=42) returns exactly 5 papers
    - load_qasper_papers with same seed returns same papers (reproducible)
    - load_qasper_papers filters to papers with >= 3 questions and <= 200 paragraphs
    - extract_gold_answers handles unanswerable, yes_no, extractive_spans, free_form_answer
    - extract_gold_answers returns empty list for empty answers
  </behavior>
  <action>
RED: Write eval/tests/test_classify.py testing all classification paths using synthetic chunks and numpy embeddings from conftest fixtures. Write eval/tests/test_dataset.py testing extract_gold_answers with mock QASPER answer structures (not requiring dataset download). For load_qasper_papers, write a test that mocks datasets.load_dataset to return synthetic data, verifying selection logic and seed reproducibility. Run tests -- must fail.

GREEN: Implement eval/classify.py with classify_failure(gold_text, all_chunks, retrieved_chunks, gold_embedding, retrieved_embeddings, similarity_threshold=0.7) following 06-RESEARCH.md Pattern 5:
1. Check CRAWL_MISS: gold_text.lower() not found as substring in any chunk["text"].lower()
2. Check RETRIEVAL_MISS: cosine sim of gold_embedding vs retrieved_embeddings all below threshold (or empty retrieved)
3. Default: SYNTHESIS_FAIL

Implement eval/dataset.py with:
- load_qasper_papers(n=5, seed=42): use datasets.load_dataset("allenai/qasper", split="test"), filter candidates with >= 3 questions and <= 200 total paragraphs, sort by question density (questions / paragraphs), sample n from top 20 using Random(seed). Return list of paper dicts.
- extract_gold_answers(qas_entry): iterate qas_entry["answers"]["answer"], check unanswerable first, then yes_no, then extractive_spans (joined with ", "), then free_form_answer. Return list of {"type": str, "text": str}.

Run tests -- must pass.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder && python -m pytest eval/tests/test_classify.py eval/tests/test_dataset.py -x -q</automated>
  </verify>
  <done>classify_failure correctly identifies all three failure modes. load_qasper_papers selects papers reproducibly by density. extract_gold_answers handles all QASPER answer types.</done>
</task>

</tasks>

<verification>
python -m pytest eval/tests/ -x -q
All 3 modules (scoring, classify, dataset) have passing tests.
</verification>

<success_criteria>
- eval/scoring.py: normalize_answer + token_f1 + score_answer all tested and passing
- eval/classify.py: classify_failure with CRAWL_MISS / RETRIEVAL_MISS / SYNTHESIS_FAIL heuristics tested
- eval/dataset.py: load_qasper_papers + extract_gold_answers tested with mocks
- All tests pass: python -m pytest eval/tests/ -x -q
</success_criteria>

<output>
After completion, create `.planning/phases/06-eval-harness/06-01-SUMMARY.md`
</output>
