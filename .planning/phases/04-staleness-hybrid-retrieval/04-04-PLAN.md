---
phase: 04-staleness-hybrid-retrieval
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/grep.py
  - backend/tests/test_grep.py
autonomous: true
gap_closure: true
requirements: [RETR-06]

must_haves:
  truths:
    - "grep_live fetches Google Workspace files (Docs/Sheets/Slides) without 403 errors"
    - "grep_live fetches binary files (PDF, TXT, MD) via alt=media as before"
  artifacts:
    - path: "backend/grep.py"
      provides: "mime-type-aware fetch_and_extract"
      contains: "export"
    - path: "backend/tests/test_grep.py"
      provides: "tests for Workspace file branching"
  key_links:
    - from: "backend/grep.py"
      to: "backend/drive.py"
      via: "reuses EXPORT_MIME_MAP or same branching logic"
      pattern: "export"
---

<objective>
Fix grep_live 403 Forbidden on Google Workspace files by adding mime-type branching to fetch_and_extract().

Purpose: grep_live currently uses ?alt=media for all files, which fails on Docs/Sheets/Slides. drive.py:export_file() already has the correct branching logic.
Output: Working grep_live that handles both Workspace and binary files.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-staleness-hybrid-retrieval/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mime-type branching to fetch_and_extract in grep.py</name>
  <files>backend/grep.py, backend/tests/test_grep.py</files>
  <behavior>
    - Test 1: fetch_and_extract with Workspace mimeType (application/vnd.google-apps.document) calls /export endpoint with mimeType=text/plain, not ?alt=media
    - Test 2: fetch_and_extract with binary mimeType (application/pdf) still uses ?alt=media
    - Test 3: grep_live passes mimeType through to fetch_and_extract
  </behavior>
  <action>
1. Modify `fetch_and_extract(file_id, access_token)` signature to accept `mime_type: str` parameter.
2. Import `EXPORT_MIME_MAP` from `backend.drive` (or duplicate the dict locally to avoid coupling — Claude's discretion, but importing is cleaner).
3. Branch logic:
   - If `mime_type in EXPORT_MIME_MAP`: use `{DRIVE_API_BASE}/{file_id}/export?mimeType={EXPORT_MIME_MAP[mime_type]}`
   - Else: use existing `?alt=media` path
4. Update `grep_live` signature to accept `mime_type: str` and pass it to `fetch_and_extract`.
5. Update the caller in `backend/chat.py` where `grep_live` is called — pass the file's mimeType from the file_list metadata. The staleness detection already has file_list available with mimeType fields.
6. Update tests: mock aiohttp to verify correct URL is constructed for Workspace vs binary files.
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder && python -m pytest backend/tests/test_grep.py -x -v</automated>
  </verify>
  <done>fetch_and_extract uses /export for Workspace files and ?alt=media for binary files. grep_live passes mimeType through. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/test_grep.py -x -v` passes with new branching tests
- `python -m pytest backend/tests/ -x` all backend tests still pass
</verification>

<success_criteria>
grep_live can fetch content from Google Docs/Sheets/Slides without 403 errors by using the /export endpoint with appropriate export mimeType.
</success_criteria>

<output>
After completion, create `.planning/phases/04-staleness-hybrid-retrieval/04-04-SUMMARY.md`
</output>
