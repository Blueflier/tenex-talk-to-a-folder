---
phase: 04-staleness-hybrid-retrieval
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/chat/StalenessBanner.tsx
  - frontend/src/components/chat/StalenessBannerList.tsx
  - frontend/src/components/chat/ChatMessage.tsx
  - frontend/src/hooks/useStream.ts
  - frontend/src/lib/db.ts
  - frontend/tests/staleness-banner.test.ts
autonomous: true
requirements: [CHAT-05]

must_haves:
  truths:
    - "Yellow staleness banner renders above assistant message for stale files"
    - "Red banner renders for deleted files (404) with no re-index button"
    - "Orange banner renders for access-revoked files (403) with Re-authenticate button"
    - "Staleness banners appear on every response that used grep_live, not just first"
    - "stale_files array persisted per message in IndexedDB, banners survive page reload"
    - "useStream parses staleness SSE events and stores stale files on the message being built"
  artifacts:
    - path: "frontend/src/components/chat/StalenessBanner.tsx"
      provides: "Single file staleness banner with three variants"
      exports: ["StalenessBanner", "StalenessInfo"]
    - path: "frontend/src/components/chat/StalenessBannerList.tsx"
      provides: "Container for multiple banners above a message"
      exports: ["StalenessBannerList"]
    - path: "frontend/src/hooks/useStream.ts"
      provides: "Extended useStream with staleness event parsing"
    - path: "frontend/src/lib/db.ts"
      provides: "Updated message schema with stale_files field"
    - path: "frontend/tests/staleness-banner.test.ts"
      provides: "Tests for banner variants and rendering"
  key_links:
    - from: "frontend/src/hooks/useStream.ts"
      to: "SSE staleness event"
      via: "parse type=staleness event, store on message"
      pattern: "type.*staleness"
    - from: "frontend/src/components/chat/ChatMessage.tsx"
      to: "frontend/src/components/chat/StalenessBannerList.tsx"
      via: "render banners above assistant content when stale_files present"
      pattern: "StalenessBannerList|stale_files"
    - from: "frontend/src/lib/db.ts"
      to: "IndexedDB messages store"
      via: "stale_files field persisted on message record"
      pattern: "stale_files"
---

<objective>
Frontend staleness banner components and useStream staleness event handling.

Purpose: Show users which files are stale/deleted/revoked before each answer, persist stale state per message so banners survive reload and scroll.
Output: Three-variant banner component, useStream staleness parsing, stale_files persisted in IndexedDB.
</objective>

<execution_context>
@/Users/josephhartono/.claude/get-shit-done/workflows/execute-plan.md
@/Users/josephhartono/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-staleness-hybrid-retrieval/04-CONTEXT.md
@.planning/phases/04-staleness-hybrid-retrieval/04-RESEARCH.md
@spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StalenessBanner components and useStream staleness event parsing</name>
  <files>frontend/src/components/chat/StalenessBanner.tsx, frontend/src/components/chat/StalenessBannerList.tsx, frontend/src/hooks/useStream.ts</files>
  <action>
    **Create `frontend/src/components/chat/StalenessBanner.tsx`:**

    Export `StalenessInfo` interface: `{ file_name: string; file_id: string; error?: "not_found" | "access_denied" | null }`.

    Single banner component with three variants per CONTEXT.md locked decisions:

    1. **Stale (no error)** -- yellow: `bg-yellow-50 border-yellow-200 text-yellow-800`. Text: "[AlertTriangle icon] {file_name} was modified after indexing -- showing live search results for this file." Right side: placeholder for Re-index button (actual ReindexButton wired in Plan 03).

    2. **Deleted (error="not_found")** -- red: `bg-red-50 border-red-200 text-red-800`. Text: "This file no longer exists in Google Drive. Old answers from this file may be outdated." No re-index button.

    3. **Access revoked (error="access_denied")** -- orange/amber: `bg-amber-50 border-amber-200 text-amber-800`. Text: "Access to {file_name} was revoked. Check your Google Drive permissions." Right side: "Re-authenticate" button (triggers re-auth flow from Phase 1).

    Props: `info: StalenessInfo`, `sessionId: string`, `onReindexComplete?: (fileId: string, indexedAt: string) => void`, `reindexSlot?: React.ReactNode` (for Plan 03 to inject ReindexButton).

    Icons from lucide-react: AlertTriangle (yellow), XCircle (red), ShieldAlert or AlertTriangle (amber).

    Per CONTEXT.md: 0 grep matches case gets extra line "No matches found -- re-index for best results". Add optional `noMatches?: boolean` prop.

    **Create `frontend/src/components/chat/StalenessBannerList.tsx`:**
    - Props: `staleFiles: StalenessInfo[]`, `sessionId: string`, `renderReindexButton?: (info: StalenessInfo) => React.ReactNode`
    - Renders one StalenessBanner per file in a flex column with gap-2
    - Positioned ABOVE the assistant message content (per CONTEXT.md)

    **Modify `frontend/src/hooks/useStream.ts`:**
    - Add `onStaleness?: (files: StalenessInfo[]) => void` callback to the options
    - In SSE parser, handle `type: "staleness"` event: call `onStaleness(event.files)`
    - Track `currentStaleFiles` in the hook's closure, set when staleness event arrives
    - Expose `currentStaleFiles` so ChatView can persist them on the message
    - After `onDone`, reset `currentStaleFiles` to empty
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>StalenessBanner renders three variants (yellow/red/orange) with correct text; StalenessBannerList renders multiple banners; useStream parses staleness events and exposes currentStaleFiles; all types compile</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stale files persistence on IndexedDB messages + ChatMessage integration</name>
  <files>frontend/src/lib/db.ts, frontend/src/components/chat/ChatMessage.tsx, frontend/tests/staleness-banner.test.ts</files>
  <behavior>
    - saveMessage stores stale_files array on the message record when provided
    - loadMessages returns messages with stale_files field intact
    - ChatMessage renders StalenessBannerList above assistant content when message.stale_files is non-empty
    - ChatMessage does NOT render banners for user messages
    - Banners render correctly from persisted data after page reload
  </behavior>
  <action>
    **Update `frontend/src/lib/db.ts`:**
    - Add `stale_files?: StalenessInfo[]` to the message record type
    - Update `saveMessage` signature: `saveMessage(sessionId, role, content, citations?, staleFiles?)`
    - When staleFiles provided, store on the record
    - loadMessages returns stale_files field as-is from IndexedDB

    **Modify `frontend/src/components/chat/ChatMessage.tsx`:**
    - Import StalenessBannerList
    - If `message.role === "assistant"` and `message.stale_files?.length > 0`, render `<StalenessBannerList>` ABOVE the message content (before the markdown/citation rendering)
    - Pass sessionId and staleFiles from the message record
    - For now, no reindexSlot (Plan 03 adds ReindexButton)

    **Also update ChatView.tsx** (minimal change):
    - In useStream callbacks: when onStaleness fires, store staleFiles locally
    - In onDone callback: pass staleFiles to saveMessage along with citations
    - Ensure staleFiles are set on the assistant message object in the messages array state

    **Create `frontend/tests/staleness-banner.test.ts`:**
    - Use vitest + @testing-library/react
    - test_yellow_banner: StalenessInfo with no error renders yellow banner with correct text
    - test_red_banner: error="not_found" renders red banner, no re-index placeholder
    - test_orange_banner: error="access_denied" renders amber banner with Re-authenticate
    - test_no_matches_line: noMatches=true adds extra "No matches found" text
    - test_banner_list: multiple StalenessInfo items render multiple banners
    - test_stale_files_persistence: mock IndexedDB, save message with stale_files, load and verify stale_files returned
  </action>
  <verify>
    <automated>cd /Users/josephhartono/src/tenex-talk-to-a-folder/frontend && pnpm test -- --run</automated>
  </verify>
  <done>stale_files persisted on IndexedDB message records; ChatMessage renders staleness banners above assistant content from persisted data; banners survive page reload; all frontend tests pass including banner variant tests</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test -- --run` -- all frontend tests pass
- `cd frontend && npx tsc --noEmit` -- no type errors
- Three banner variants render with correct colors and text
- stale_files stored on messages in IndexedDB
</verification>

<success_criteria>
- Three-variant staleness banner (yellow/red/orange) renders correctly
- useStream parses staleness SSE events
- stale_files persisted per message in IndexedDB
- Banners render above assistant messages from persisted data
- All frontend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-staleness-hybrid-retrieval/04-02-SUMMARY.md`
</output>
